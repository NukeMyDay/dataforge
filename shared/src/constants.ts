export const SUPPORTED_LOCALES = ["de", "en", "nl"] as const;

export const DEFAULT_PAGE_SIZE = 50;
export const MAX_PAGE_SIZE = 500;

export const DEGREE_TYPES = [
  "bachelor",
  "master",
  "phd",
  "mba",
  "certificate",
  "diploma",
  "other",
] as const;

export const DELIVERY_MODES = ["on-campus", "online", "hybrid"] as const;
